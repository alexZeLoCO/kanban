import { useDrag } from 'react-dnd';

export type TaskProps = {
    id: number;
    content: string;
};

export const Task: (props: TaskProps) => JSX.Element = ({ id, content }) => {
  return (
    <></>
  );
};
